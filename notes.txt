BACKEND
pyinstaller backend_script.py --onedir --noconsole --add-data "utils;utils" --add-binary "ffmpeg.exe;." --add-binary "ffprobe.exe;."

EXE NAME:
backend_script-x86_64-pc-windows-msvc.exe


new test:
pyinstaller backend_script.py --onedir --noconsole --collect-submodules utils --add-binary "ffmpeg.exe;." --add-binary "ffprobe.exe;."



PLAN:
1) Check if user has HEVC installed on their computer on startup
2) Check if clip is H.265 via helper function (using python stream function)

If user has HEVC installed at the beginning ---> override all h.264 decoding
If user does NOT have HEVC installed:
    -> If imported episode is h.265, then return a boolean to decode clips to h.264, disable all clip preview and encode clips to h.264
    -> If imported episode isn't, then proceed as normal and enable all clip preview
 